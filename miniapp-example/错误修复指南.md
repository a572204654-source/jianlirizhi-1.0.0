# Word导出错误修复指南

## 🔴 最新错误：服务器错误 (statusCode 非 200)

```
❌ 服务器错误(env: Windows,mp,1.06.2504060; lib: 3.11.0)
导出Word失败： Error: 服务器错误
at Object.success (word-export.js:156)
```

### 🎯 问题分析

这个错误说明：
- ✅ 前端代码正确（已经使用 `downloadFile`）
- ✅ 域名配置正确（请求已发出）
- ❌ **后端返回了错误响应**（statusCode 不是 200）

但是 `downloadFile` **无法显示后端的详细错误信息**！

---

## 🔧 立即解决：查看详细错误

### 步骤1：使用调试版代码

将 `调试版-Word导出.js` 临时替换你的导出代码：

```javascript
// 1. 将 调试版-Word导出.js 复制到 utils 目录
// 2. 修改引入路径（临时）
const { exportWord } = require('../../utils/调试版-Word导出')

// 3. 调用导出
exportWord(logId)
```

### 步骤2：查看控制台

调试版会先用 `wx.request` 检查接口，显示**后端的详细错误信息**：

```
🔍 步骤1：检查API接口
📍 URL: https://your-domain.com/api/supervision-logs/123/export
🔑 Token: eyJhbGciOiJIUzI1NiIs...
❌ 服务器返回错误: {"code":404,"message":"日志不存在"}
```

### 步骤3：根据错误信息修复

常见的后端错误：

#### ❌ 错误1：日志不存在 (404)
```json
{"code": 404, "message": "日志不存在"}
```

**原因**：
- 日志ID不存在
- 日志已被删除
- 日志ID格式错误

**解决**：
- 检查传入的 `logId` 是否正确
- 确认数据库中存在该日志

#### ❌ 错误2：无权访问 (403)
```json
{"code": 403, "message": "无权操作"}
```

**原因**：
- 该日志不属于当前用户
- 权限验证失败

**解决**：
- 确认用户有权限访问该日志
- 检查后端的权限验证逻辑

#### ❌ 错误3：认证失败 (401)
```json
{"code": 401, "message": "Token无效或已过期"}
```

**原因**：
- Token过期
- Token无效
- 未传递Token

**解决**：
- 重新登录获取新Token
- 检查Token存储位置
- 确认Token正确传递

#### ❌ 错误4：数据库错误 (500)
```json
{"code": 500, "message": "数据库查询失败"}
```

**原因**：
- 数据库连接失败
- SQL语句错误
- 数据表不存在

**解决**：
- 检查后端日志
- 确认数据库连接正常
- 运行数据库初始化脚本

#### ❌ 错误5：Word生成失败 (500)
```json
{"code": 500, "message": "Word文档生成失败"}
```

**原因**：
- docx模板文件不存在
- Word生成逻辑错误
- 文件系统权限问题

**解决**：
- 检查 `docx/template.docx` 是否存在
- 查看后端错误日志
- 确认文件写入权限

---

## 🎯 完整的调试流程

### 1. 先用调试版查看详细错误

```javascript
// 在 profile-logs.vue 中
import { exportWord } from '@/utils/调试版-Word导出.js'

methods: {
  async handleExportWord() {
    try {
      await exportWord(this.logId)
    } catch (error) {
      console.error('导出失败:', error)
      // 查看控制台的详细错误信息
    }
  }
}
```

### 2. 根据错误修复后端

假设看到错误：`{"code": 404, "message": "日志不存在"}`

检查：
- 数据库中是否存在该日志
- 日志的 status 是否正常（不是软删除）
- user_id 是否匹配

### 3. 修复后换回正式版

问题解决后，恢复使用正式的导出代码：

```javascript
const { exportWord } = require('../../utils/word-export')
```

---

## 📋 其他常见错误

### 1. ❌ 域名未配置（最常见）
使用 `wx.downloadFile` 时，域名必须在微信公众平台配置

### 2. ❌ 使用了错误的API方法
可能使用了 `wx.request` 而不是 `wx.downloadFile`

### 3. ❌ Token未正确传递
认证信息未正确添加到请求头

### 4. ❌ 接口路径错误
后端接口路径与前端调用不匹配

---

## ✅ 解决方案

### 方案一：配置域名（推荐）

如果你想使用 `wx.downloadFile`（最简单的方式）：

#### 步骤1：登录微信公众平台
1. 访问：https://mp.weixin.qq.com
2. 使用小程序管理员账号登录

#### 步骤2：配置域名
1. 点击左侧菜单：**开发管理**
2. 点击：**开发设置**
3. 找到：**服务器域名**
4. 点击 **downloadFile合法域名** 后的 **修改**
5. 添加你的后端域名（必须是https）：
   ```
   https://your-domain.com
   ```
6. 保存并等待生效（通常立即生效）

#### 步骤3：使用正确的代码

在你的 `profile-logs.vue` 中替换导出方法：

```javascript
// ✅ 正确的导出方法
handleExportWord() {
  const logId = this.currentLogId // 根据你的实际变量名修改
  
  // 显示加载
  wx.showLoading({
    title: '正在导出...',
    mask: true
  })

  // 获取token
  const token = wx.getStorageSync('token')
  
  if (!token) {
    wx.hideLoading()
    wx.showToast({
      title: '请先登录',
      icon: 'none'
    })
    return
  }

  // 你的后端地址
  const BASE_URL = 'https://your-domain.com' // 修改为实际地址

  // 下载Word文档
  wx.downloadFile({
    url: `${BASE_URL}/api/supervision-logs/${logId}/export`,
    header: {
      'Authorization': `Bearer ${token}`
    },
    success(res) {
      wx.hideLoading()
      
      console.log('下载响应:', res) // 用于调试
      
      if (res.statusCode === 200) {
        // 成功，打开文档
        wx.openDocument({
          filePath: res.tempFilePath,
          fileType: 'docx',
          showMenu: true,
          success() {
            wx.showToast({
              title: '导出成功',
              icon: 'success'
            })
          },
          fail(err) {
            console.error('打开文档失败:', err)
            wx.showToast({
              title: '打开失败',
              icon: 'none'
            })
          }
        })
      } else if (res.statusCode === 401) {
        // Token过期
        wx.showModal({
          title: '登录已过期',
          content: '请重新登录',
          showCancel: false
        })
      } else if (res.statusCode === 404) {
        // 日志不存在
        wx.showToast({
          title: '日志不存在',
          icon: 'none'
        })
      } else {
        // 其他错误
        wx.showToast({
          title: `导出失败(${res.statusCode})`,
          icon: 'none'
        })
      }
    },
    fail(err) {
      wx.hideLoading()
      console.error('下载失败:', err)
      
      // 详细的错误提示
      if (err.errMsg && err.errMsg.includes('domain')) {
        wx.showModal({
          title: '域名未配置',
          content: '请在微信公众平台配置 downloadFile 合法域名',
          showCancel: false
        })
      } else {
        wx.showToast({
          title: err.errMsg || '下载失败',
          icon: 'none'
        })
      }
    }
  })
}
```

---

### 方案二：使用 wx.request（无需配置域名）

如果你暂时无法配置域名，可以使用 `wx.request`：

```javascript
handleExportWord() {
  const logId = this.currentLogId
  
  wx.showLoading({
    title: '正在导出...',
    mask: true
  })

  const token = wx.getStorageSync('token')
  const BASE_URL = 'https://your-domain.com'

  wx.request({
    url: `${BASE_URL}/api/supervision-logs/${logId}/export`,
    method: 'GET',
    responseType: 'arraybuffer', // 重要：指定响应类型
    header: {
      'Authorization': `Bearer ${token}`
    },
    success(res) {
      wx.hideLoading()
      
      if (res.statusCode === 200) {
        // 获取文件系统管理器
        const fs = wx.getFileSystemManager()
        
        // 生成临时文件路径
        const fileName = `监理日志_${logId}_${Date.now()}.docx`
        const filePath = `${wx.env.USER_DATA_PATH}/${fileName}`
        
        // 写入文件
        fs.writeFile({
          filePath: filePath,
          data: res.data,
          encoding: 'binary',
          success() {
            // 打开文档
            wx.openDocument({
              filePath: filePath,
              fileType: 'docx',
              showMenu: true,
              success() {
                wx.showToast({
                  title: '导出成功',
                  icon: 'success'
                })
              },
              fail(err) {
                console.error('打开文档失败:', err)
                wx.showToast({
                  title: '打开失败',
                  icon: 'none'
                })
              }
            })
          },
          fail(err) {
            console.error('写入文件失败:', err)
            wx.showToast({
              title: '保存失败',
              icon: 'none'
            })
          }
        })
      } else {
        wx.showToast({
          title: `导出失败(${res.statusCode})`,
          icon: 'none'
        })
      }
    },
    fail(err) {
      wx.hideLoading()
      console.error('请求失败:', err)
      wx.showToast({
        title: '导出失败',
        icon: 'none'
      })
    }
  })
}
```

---

## 🔍 调试步骤

### 1. 检查BASE_URL是否正确

```javascript
// 在导出方法开始时添加
console.log('BASE_URL:', BASE_URL)
console.log('完整URL:', `${BASE_URL}/api/supervision-logs/${logId}/export`)
```

### 2. 检查Token是否有效

```javascript
// 在导出方法中添加
const token = wx.getStorageSync('token')
console.log('Token:', token ? '存在' : '不存在')
console.log('Token长度:', token ? token.length : 0)
```

### 3. 检查接口路径

确认你的后端接口路径：
- ✅ `/api/supervision-logs/:id/export`
- ❌ `/api/v1/supervision-logs/:id/export`（如果是这个，需要修改前端URL）

### 4. 使用开发工具测试

在微信开发者工具中：
1. 点击右上角 **详情**
2. 勾选 **不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书**
3. 重新测试导出功能

如果开发工具中能成功，说明是域名配置问题。

### 5. 查看网络请求

在微信开发者工具中：
1. 点击 **调试器** 标签
2. 切换到 **Network** 面板
3. 点击导出按钮
4. 查看请求详情：
   - 请求URL是否正确
   - 状态码是多少
   - 响应内容是什么

---

## 📝 常见错误对照表

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| `downloadFile:fail invalid url domain` | 域名未配置 | 在微信公众平台配置域名 |
| `statusCode: 401` | Token无效或过期 | 重新登录获取新token |
| `statusCode: 404` | 日志不存在或接口路径错误 | 检查logId和接口路径 |
| `statusCode: 500` | 后端服务错误 | 查看后端日志 |
| `request:fail` | 网络请求失败 | 检查网络连接 |
| `openDocument:fail` | 文件打开失败 | 检查文件格式和路径 |

---

## 🎯 完整的可用代码（复制粘贴即可）

### 如果你使用原生微信小程序：

```javascript
// 在你的 profile-logs.vue 或 .js 文件中

// 配置项（修改为你的实际值）
const CONFIG = {
  BASE_URL: 'https://your-domain.com', // 你的后端地址
  TOKEN_KEY: 'token'                    // token在storage中的key
}

// 导出Word方法
function exportWord(logId) {
  // 参数检查
  if (!logId) {
    wx.showToast({
      title: '日志ID不存在',
      icon: 'none'
    })
    return
  }

  // 显示加载
  wx.showLoading({
    title: '正在导出...',
    mask: true
  })

  // 获取token
  const token = wx.getStorageSync(CONFIG.TOKEN_KEY)
  
  if (!token) {
    wx.hideLoading()
    wx.showToast({
      title: '请先登录',
      icon: 'none'
    })
    return
  }

  // 构建URL
  const url = `${CONFIG.BASE_URL}/api/supervision-logs/${logId}/export`
  
  console.log('导出URL:', url) // 调试用

  // 下载文件
  wx.downloadFile({
    url: url,
    header: {
      'Authorization': `Bearer ${token}`
    },
    success(res) {
      wx.hideLoading()
      console.log('下载响应:', res)
      
      if (res.statusCode === 200) {
        // 打开文档
        wx.openDocument({
          filePath: res.tempFilePath,
          fileType: 'docx',
          showMenu: true,
          success() {
            wx.showToast({
              title: '导出成功',
              icon: 'success'
            })
          },
          fail(err) {
            console.error('打开失败:', err)
            wx.showToast({
              title: '打开失败',
              icon: 'none'
            })
          }
        })
      } else {
        // 错误处理
        let message = '导出失败'
        if (res.statusCode === 401) {
          message = '登录已过期，请重新登录'
        } else if (res.statusCode === 404) {
          message = '日志不存在'
        }
        wx.showToast({
          title: message,
          icon: 'none'
        })
      }
    },
    fail(err) {
      wx.hideLoading()
      console.error('下载失败:', err)
      
      let message = '下载失败'
      if (err.errMsg && err.errMsg.includes('domain')) {
        message = '请配置downloadFile合法域名'
      }
      
      wx.showToast({
        title: message,
        icon: 'none',
        duration: 3000
      })
    }
  })
}

// 导出供其他文件使用
module.exports = {
  exportWord
}
```

### 在页面中使用：

```javascript
// profile-logs.vue 或 profile-logs.js

const { exportWord } = require('../../utils/word-export') // 路径根据实际调整

Page({
  data: {
    logId: null
  },

  onLoad(options) {
    this.setData({
      logId: options.id
    })
  },

  // 导出按钮点击事件
  handleExportWord() {
    exportWord(this.data.logId)
  }
})
```

---

## ✅ 配置检查清单

使用前请逐项检查：

- [ ] 已修改 `BASE_URL` 为实际后端地址
- [ ] 后端地址是 `https://` 开头（不能是http）
- [ ] 已在微信公众平台配置 downloadFile 合法域名
- [ ] Token 存储的 key 名称正确（默认是 'token'）
- [ ] 接口路径正确（`/api/supervision-logs/:id/export`）
- [ ] 日志ID存在且有效
- [ ] 后端服务正常运行
- [ ] 网络连接正常

---

## 🆘 仍然失败？

### 1. 在开发工具中测试

先在开发工具中测试，关闭域名校验：
- 详情 → 不校验合法域名

如果开发工具中能成功：
✅ 说明代码正确，只需配置域名即可

### 2. 查看完整错误信息

在 `fail` 回调中添加：
```javascript
fail(err) {
  console.log('完整错误:', JSON.stringify(err))
  // 复制这个错误信息
}
```

### 3. 测试后端接口

使用 curl 或 Postman 测试后端：
```bash
curl -X GET \
  "https://your-domain.com/api/supervision-logs/1/export" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -o test.docx
```

如果能下载到 `test.docx` 文件，说明后端正常。

### 4. 联系技术支持

如果以上方法都不行，请提供：
1. 完整的错误信息
2. 后端地址（脱敏）
3. 接口路径
4. 是否配置了域名
5. 在开发工具中是否成功

---

**更新时间**: 2024-11-07  
**适用场景**: 微信小程序 Word 导出功能调试

